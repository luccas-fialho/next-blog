# SERVER

How to start the site:

```sh
# Must have Node installed
# Install all the packages
npm i
# Set the .env.local
npm run migrate
npm run seed # Seed is optional

# Next build
npm run build
npm start #just for tests
```
